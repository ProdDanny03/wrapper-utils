[project]
name = "wrapper-utils"
version = "0.1.0"
description = "A couple of handy decorator functions"
authors = [
    { name="Danny" }
]

dependencies = [
    "numpy"
]

[build-system]
requires = ["setuptools>=80.9.0"]
build-backend = "setuptools.build_meta"